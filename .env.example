# STAGING ONLY. Copy to .env.local and never commit populated values.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Synthetic, local-only mode. Never enable in a production deployment.
VITE_ENABLE_DEMO_MODE=true
